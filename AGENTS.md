# Instruções para o assistente

## Git workflow
- Após qualquer alteração no `index.html`, rodar automaticamente:
  1. `git add .`
  2. `git commit -m "descrição da mudança"`
  3. `git push`
- Exceto quando o usuário pedir para validar primeiro.
